{
  "name": "dkp-arizalarni-boshqaruv-tizimi",
  "version": "0.1.0",
  "description": "DKP Applications Management System — Google Apps Script Web App for the State Cadastre",
  "private": true,
  "scripts": {
    "lint": "eslint \"src/**/*.js\" \"test/**/*.js\"",
    "test": "node --test \"test/*.test.js\"",
    "push": "clasp push",
    "deploy": "clasp deploy"
  },
  "keywords": [
    "google-apps-script",
    "cadastre",
    "dashboard"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0"
  }
}
